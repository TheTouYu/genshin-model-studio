# 甘雨 v15 — 局部先生成+整体拼装（效率）+ 无缝方格网（质量）

## 用户指导 → 落地
1. **每次全量跑重复 → 局部生成+拼装**：
   - `scripts/run-gms-parts.sh <out> <part.js>...`：每个 part **独立秒级生成**（`parts/ganyu-p1-body.js`、`parts/ganyu-p2-top.js`），
     导出局部作品 → **拼装** `{version:3, strokes: concat(parts), options}` → POST → `items.json`；
     再以 `window.gms.import`（**base64 分块加载**，解决大作品 IPC 上限）注入页面 → 全量预览截图。
   - **验证**：拼接结果与整跑一致（部分拼接 = **6376 笔 / 6376 件**），无需每次重跑全模即可迭代单个局部。
2. **面与面之间有间隔（大问题）→ 算法修正**：
   - `surface()` 单元从 `w*0.94/h*0.97`（原为防毛刺而缩边）改为 **`w*1.0/h*1.0` 满格共边**——面板不再留缝；
   - 面板用 10009003 平面 + 正确法线朝向，邻格共边、间距≈0。

## 数据（拼装版 = v15）
- **6376 面**（10009003×6309 + 10009008×58 + 10009002×3 + 10009009×2 + 10009006×4），≤30000。
- 量化 IoU = **0.6865**（与 v14 整跑 0.686–0.695 一致区间；历史最优 0.6951）。
- 关键行（腿 1.7–5.6%、膝/髋 3–6%、腰 0.6–2.1%）≤12% 达标；手/颈/发顶行仍待局部优化（得益于拼装，**现在只跑 part2 即可迭代**）。

## 交付物
- `scripts/run-gms-parts.sh`（局部生成+拼装流水线）、`scripts/parts/ganyu-p1-body.js`、`parts/ganyu-p2-top.js`
- `scripts/draw-ganyu-soccer-v15.js`（无缝方格网源脚本）、`delivery/ganyu-v15/`（work/items/视图/clean）
- 历史版本 `vmtoqmaytwlao`

## 下一步（目标 active）
1. **局部迭代闭环验证**：只改 `p2`（发/鞋/图案）→ `run-gms-parts.sh out p1 p2`（~1 分钟）→ 对比；
2. 用局部闭环攻 h0.47-0.62（手/短裤）与 h0.82-0.97（发顶），复测至 **IoU≥0.70、关键行≤12%**；
3. 达标后：REPORT 更新 + 技能同步 + 目标完成。
