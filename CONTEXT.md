# Genshin Model Studio

把"画线草图"转化为"3D 静态模型"（.gil/.gia）的画线建模工具。用户在 2D 画布上画轮廓，系统拟合成形状并生成可预览、可导出的 3D 元件。

## Language

**笔画 (Stroke)**:
一次画布绘制（一个点集 + 一组属性），是建模的最小单位。点集是 3D 世界坐标，属性包括渲染语义、朝向、整体变换。
_Avoid_: 线条、图形

**变换 (Transform)**:
笔画的整体摆放属性：位置偏移（米，叠加在点集默认位置上）与旋转（度，最终欧拉角）。笔画在空间中的"摆放"与"形状"由此分离——这是核验与整体编辑（移动/旋转）的直接操作对象。
_Avoid_: 位置、角度（指单个分量时可用，指整体时用变换）

**视图 (View)**:
画布当前的投影平面（正视图 front=XY / 俯视图 top=XZ / 侧视图 side=YZ）。画布显示 = 所有笔画在该视图下的投影，与 3D 预览一致。
_Avoid_: 视角（指相机朝向时保留，指画布投影时用视图）

**拟合 (Fit)**:
服务端把笔画点集识别为形状（圆/椭圆/矩形/线段/曲线）并计算尺寸的过程。
_Avoid_: 识别（拟合含尺寸计算，识别只是其中一步）

**柱体 (Solid)**:
带厚度的实体笔画（挤出/车削渲染），有高度属性。
_Avoid_: 块、体

**杆 (Rod)**:
无厚度的线状笔画（按轮廓分段渲染）。
_Avoid_: 线（线是形状类别，杆是渲染语义）

**朝向 (Axis)**:
笔画形状在空间中的默认取向（front/up/side），决定未加变换时形状的摆放。
_Avoid_: 方向（与"旋转"混用；朝向是静态语义，旋转是变换）

**旋转副本 (Rotation copy)**:
gms.rotate 生成的等角分布副本（点集已旋转 + 带变换属性）。源笔画是"源"，副本通过变换表达 3D 语义。
_Avoid_: 拷贝、复制件

**标定 (Calibration)**:
画布像素与米的比例（px/m），决定模型的绝对尺寸。
_Avoid_: 缩放（标定是全局约定，缩放是局部属性）
