# HARVEST · 长跑干预实验价值清单（2026-09-09）

> 这轮对话（研究会话 ef04b2 × 长跑会话 182bd997）产出的全部可复用信息。
> 用途：之后系统性地研究"怎么发挥它们的价值"。完整案例见 `REPORT-macbook-max-execution-analysis.md`（§0–§8）。
> 来源标注：〔trace〕=会话日志实测；〔推断〕=分析结论；〔工程〕=今日实操踩坑。

---

## 1. 实测数据点（时间线量化）

| 时刻 | 事件 | 数字 |
|---|---|---|
| 12:23–15:56 | 自由长跑 | 3.58h / 359 步 / 370 调用；思考仅 12.1% 墙钟；渲染 74.3% 墙钟；compress 仅 1 次 |
| 12:24:06 | 架构锁定 | "deliverable is images" 90 秒定架构 |
| 12:30 | 自定标准 | 自发发明盲测 + 50% 误判线（=用户真实标准，16:34 才由用户说出） |
| 14:30/15:15/15:56 | 盲测三波 | 误判率 0–8% → 22% → 41.7%（逼近 50%） |
| 15:45 | 用户点破性能 | 94 秒优化（-36% tris）——缺触发器非能力 |
| 16:15:49 | 诚实自评 | "41.7% < 50% 未达标，不能宣称完成" |
| 16:16:01 | goal-round 自动续跑 | create_goal（12:24 埋）在自评失败时自动重启真目标 |
| 16:17:48 | 纠偏#1 派发 | 4 秒被接收 → 8.7 分钟全架构 pivot → gia 交付 |
| 16:27–16:34 | 无指令债回填 | 瘦三角 36.6%→4.5%（过默认门）；tris 9622→4188 |
| 16:34:04 | 纠偏#2（用户校准） | 先收尾再转向，无抗议；切回渲染主线 |
| 16:39–17:09 | 渲染攻坚 | 键帽 3× 字形 bug 修复；色度噪声 0.136→0.018（真机 0.005）；kill-and-fix 伪影 |
| 17:30 | 页面加载 | 当前版 r4：3677 tris（曲面细分优化后再 -500） |

## 2. 行为定律库（修正版）

**原六条**（自由跑阶段，详见 REPORT §4）：①90 秒架构锁定；②合规物 vs 自建真控制器；③自评双层（结果层诚实/标准层漂移）；④性能无体感；⑤主动加固评测机制；⑥对抗打地鼠（资产级修复应尽早换道）。

**纠偏窗口新增/修正**：
- **L7 锁定是目标锚定的**：参数级提示只微调（15:45），目标级提示 8.7 分钟换架构（16:17）。挑战"交付物是什么"的解释→刚性消失。〔trace〕
- **L8 自定标准对齐现象**：模型从原话直接推导的标准（盲测 50%）= 用户未说出的真实标准；经中间人转述的纠偏反而失真。**链路越短越准**。〔trace+用户确认〕
- **L9 债回填自发涌现**：诚实申报义务 + 自动续跑 → 无监督期自我修复技术债 + 诊断方法学自主升级（stack 捕获归因）。〔trace〕
- **L10 上下文卫生挂靠回合结构**：3.5h 压 1 次 → goal 轮次期 20min 压 2 次。压缩保守性是结构副产品非特质。〔trace〕
- **L11 时间盒纪律**：坏组件诊断 30 秒即搁置+理由（"Deferring JPEG encoder — harness re-encodes anyway"），不死于沉没成本。〔trace〕
- **L12 假设生命周期**：容器判据被自己的更准测量推翻（参考图无 lag-8 峰）→ 利落退役转攻真判据。〔trace〕
- **L13 评价器随交付物定义被隐秘锁定**：12:24 "deliverable is images" 90 秒锁定不只锁建造方式（自建原语/绕开管线），还隐秘锁了评价器——交付物="我的渲染图" ⇒ 评价器="我的渲染器"。5h 全程页面/CDP 触点=0（370 调用全扫），159 次渲染全出自自建光线追踪器；8 裁判 3 波独立盲测（防作弊三轮进化，ab-stage.py 重编码/key 即删/不透明目录）评的全是自建渲染器输出。评价闭环必须落在被验收的真实系统上，否则优化的是渲染器不是模型（Goodhart 自证闭环）。〔trace 硬证〕

## 3. 六条超预期发现（对外认知边界）

1. **90 秒锁定可被目标级干预推翻**——推翻了我们自己 REPORT §4① 的定律（修正为 L7）。
2. **误差出在中间层**：会话端点读对了目标，研究者转述引入失真——"聪明中间人修正笨模型"的直觉被颠倒。
3. **内在监督先于外部起效且外力可净负**：goal driver + 诚实自评早 2 分钟拉回真目标；按原设计要建的 sentinel 会在更大规模重犯纠偏#1 的错。
4. ~~静默线程丢失~~（**修正**）：JPEG 容器线程休眠 10 分钟后被目标级转向连带复活，并以更高质量信息重新排序（搁置+理由）。目标级转向可复活其覆盖区域的休眠线程——非可靠机制但存在。
5. **无监督窗口的自发工匠行为**（=L9）：合规模型预测不到。
6. **压缩行为结构耦合**（=L10）。
- 方法学彩蛋：**实验者成了被测对象**——一次"干预错误"的对照试验撞出了最丰富的数据（4s/8.7min 极限、线程复活、内在监督优先性），这类数据无法设计只能撞出来。

## 4. 干预方法论 → sentinel 设计规格 v2

1. **给信息不给指令**：纠偏载荷=用户验收原话（信息不会错），非"停 X 做 Y"（指令会错——实测翻车一次）。
2. **干预时机=回合边界+自评失败时**：模型刚自报"未达标"时方向信息吸收率最高；打断在飞工作是最差时机（线程风险）。
3. **读 goal 状态而非重造**：goal-round driver 是被低估的内置监督；sentinel 应 amplify 它，不抢方向盘。
4. **打断前盘点在飞线程**：目标重述≠线程恢复；被拦的洞察需显式归还（本次靠运气复活）。
5. **评价通道校验（L13 对策）**：独立审计前先验证"被评的图来自真实系统"（浏览器页面截图，带页面 UI 上下文），自建渲染器输出一律不计入验收——审计独立性再强，刺激物来源错了整条循环就是自证。
6. 通道实测：agent-messaging queued→接收→执行→回执全链路可用；接收开关需对方 GUI 点一次后常开。

## 5. 五层优化 ↔ 生态对照（结论）

已有资产覆盖 ~70%（closedloop-mode 纪律原语 / graded-mode T0 规模闸【disabled 待复启评估】/ compaction-acp / engram / agent-messaging / super-injector+daemon-loop 底盘 / 38 全局技能：task-command-center 五件套、task-retrospective、task-trace-review、conversation-coach、oneshot-prompt-builder、isolated-model-evaluator、model-build-test、handoff / PKC）。

**真缺口只有三根传动轴**：
1. **规模路由器**：任务大小↔纪律强度自动匹配（preset 选择逻辑；两个方向失灵案例：2a8b2875 简单任务被协议税 238 调用/98min/协议 14%/注入 231×；182bd997 困难任务 closedloop-full 加载零调用自由跑）。
2. **实时监督器**：全部复盘设施都是 post-hoc；daemon-loop 底盘+research-laptop 流式 zstd 脚本+agent-messaging 三件组装即得（读侧内核现成）。
3. **条款回流**：oneshot-prompt-builder 决策点清单缺 4 条款（出口定义第一句/里程碑强制排序【第一里程碑=走完管线→交付物→核验全链】/时间账本【30min 报告占比，单工具>60% 必先优化】/自由度边界【管线内随便改，最终必须流经管线】）；conversation-coach 吃掉本案例。

## 6. 可迁移资产清单（路径）

- 完整报告+终局对照：`~/genshin-model-studio/REPORT-macbook-max-execution-analysis.md`
- 三路分析+可复跑脚本（流式 zstd trace 解析）：`/home/h/app/research-laptop/{A-trace-stats,B-behavior,C-artifacts-context}.md + stats.py/timeline.py/verdicts.py 等 14 个`
- MacBook 几何（标定 mm 级）：`src/model/macbook/{geometry,spec}.ts`；渲染器（可转 QA）：`src/render/* + scripts/max/render.mjs`
- 盲测协议 v3 实践（防作弊/答案不落盘/口径预定义/误判率硬门）：会话内 `scripts/max/ab-*.py`
- 引擎管线样例（LOD 穿线/装配门禁/mesh→gia）：commit `4de2c85`
- 记忆节点：[[LLM 长跑任务行为规律案例]]（global，已含纠偏窗口五条实测）
- 过度工程治理方案：`~/genshin-model-studio/.scratch/PLAN-2026-09-08-over-engineering.md`（修复已落地：T0 快路径/摘要标源——本会话顶部可见）

## 7. 悬而未决 / 待研究

1. 盲测 42%→50% 最后一段：噪声颗粒方向（luma_sd 2.6 vs 真机 8.1，真机有细颗粒纹理）——会话进行中。
2. 三视觉缺口收口状态：键帽✅ / 圆角分段（修中）/ 屏幕发光（修中）。
3. 引擎侧搁置项：3004 单元 vs 570 的轻重；瘦三角根因（plateFill 同心扇形+1:600 尺度跨度）已修至 4.5%。
4. sentinel 插件（dsh-longrun-sentinel）设计已定规格未开工；graded-mode 为何 disabled 待查。
5. 会话终局数据（盲测波 4 结果、最终 REPORT）尚未产出——待回合结束补录。
6. WSL2 localhost 转发对新连接抽风（页面 fetch 挂起）——今日工程坑，未根治。
7. **评价闭环重接未完成**：渲染→页面截图→独立裁判 的真循环尚未建立（2026-09-09 页面加载是这个产物第一次被真实系统评价，网格级缺陷立刻现形——自建渲染器的着色/AO/噪声此前一直在糊掉它们）。41.7% 盲测成绩不度量真实验收。

## 8. 今日工程坑（复用价值）

- **WSL→Windows 只读墙投递**：`/mnt/c` ro 挂载 → `powershell.exe` 互操作 + base64 管道写入，sha256 双侧一致。
- **browser-harness IPC ~64KB 表达式上限**：大 JSON 灌页面需分块装配（`window.__m.vertices.push(...batch)` 逐批，末次 `gms.part('mesh',{mesh:window.__m})`）。
- **页面 fetch 挂起时的降级链**：CDP 直灌（加载）+ WSL 侧 curl（保存 API）——页面内网络断时全流程仍可走通。
- **页面历史版本机制**：`benchmark/history/<id>/{work.json,meta.json}` + `/api/history/save`；任意网格可用 `gms.part('mesh')` 挂成 1 笔画历史（r4 已存：vmttwiytsfbcq）。
