先按以下规格在场景中构造一座长凳（单位：米；position 是元件中心坐标；y=0 为地面；rotation 均为 [0,0,0]）：

- 凳面：长方体（10009001），scale=[1.8, 0.1, 0.5]，position=[0, 0.55, 0]
- 左腿：长方体（10009001），scale=[0.1, 0.5, 0.1]，position=[-0.8, 0.25, 0]
- 右腿：长方体（10009001），scale=[0.1, 0.5, 0.1]，position=[0.8, 0.25, 0]

然后对整座长凳施加变换：先绕 Y 轴旋转 90°（右手定则，即 (x,z) → (-z, x)），再平移 (+1.0, 0, -0.5)。

输出变换后全部 item 的 structure.json 超集 JSON（name + items，共 3 个 item）。
长方体旋转 90° 后请用等价的 scale 轴交换 + rotation 表示（保持轴对齐，rotation 写 [0,0,0]）。
