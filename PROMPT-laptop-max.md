# MacBook Pro 14" —— 以假乱真（最高难度）

用本项目工具，做一个 CG 渲染级别的 MacBook Pro 14 英寸建模。
**目标：** 以假乱真 —— 视觉上，在 0.5–1 m 的观察距离下，无法分辨它是建模还是真机。

**工具是你的，随便改**：仓库里的建模引擎、CLI、预览页、part 库都是你的工具箱——不好用、不够用就直接改它、升级它、补新原语，别绕着限制走。唯一约束是零依赖（zero-dependency）：不引入第三方库，一切自己实现。改完跑测试、留记录。

**先把标准拉到最高**（这部分不给任何人看）：
- 花时间想清楚：什么叫做到了世界级的「像」？为这件事写一份 5–7 类的评分表。
- 用这份表逐轮自评；任何一类没拿满分，就重新做。

**然后每一轮都问自己：现在最弱的一环是哪里？**
把它做到极限，再找下一个。仓库里有你需要的一切（真机参考档案、官方图、生成图、既有产物——都是预置基线），自己找、自己判断，不要问我。

**交付前自己把门（验收）**：同角度 A/B 盲测 —— 6 张自渲染图与 6 张参考图混排去标识，让独立模型判断哪些是渲染；误判率没到随机水平（50%），就是没做到，不许宣称完成。

做完直接输出结论：现在最弱的一环是什么、为什么、下一轮做什么。maximum effort。
