# r9 报告：用户点名的四处细节修复 + 六张图重出

- 时间：2026-09-10 07:0x–07:3x（本地）
- 通道：`http://localhost:8787/draw/photo.html`（three.js r160 + PBR + 程序化影棚 PMREM + 后处理），CDP Edge 152 @ `127.0.0.1:9222`
- 网格：`web/draw/macbook-current.json` 76,333 tris / 48,090 verts（开盖 100°）、`macbook-closed.json` 76,291 tris（闭合）
- 回归：`npm test` **252/252**
- 看图页：`http://localhost:8787/draw/v8.html`（协议 6 张 + 修复特写 6 张 + 开合盖序列 4 帧）

## 1. 用户逐条反馈 → 处置

| # | 用户原话（要点） | 根因 | 修法 | 验证 |
|---|---|---|---|---|
| 1 | 「关盖的情况，屏幕还发光」 | ① 页面从不按开合角熄屏；② 闭合网格**没有 `lid` 元数据**（`page-mesh.mjs` 用 0° 姿态当参考，闭合网格一个顶点都不"动"）→ 页面根本不知道是合盖 | 屏幕自发光随角度淡出（0° 全灭、≥12° 全亮）；`page-mesh.mjs` 参考姿态改为「与输出姿态不同的那个角度」 | 合盖渲染彩色像素（sat>40）**8914 → 23** |
| 2 | 「连接处的圆柱，侧面可以看到是没做细节处理」 | 转轴是 Ø6 **开口管**，直接摆在台面之上（轴心 11.8mm + 半径 3mm = 顶部 14.8mm > 台面 11.5mm），侧视可见空心管口 | 台面后缘挖转轴槽（286×4.4mm，深 1.2mm，槽底 + 槽壁），筒降到槽内（轴心 9.6mm、Ø4、长 280mm）并加**两端端盖** | 开盖/合盖两个角度特写（`qa/hinge-*.png`）均无外露管口 |
| 3 | 「这个渲染的页面，没有接入动画，然后看开盖的情况」 | 页面只有静态视角切换，没有开合盖 | `__photo.animLid(to,ms)` / `toggleLid()`（easeInOutQuad + LIVE 快渲染）、UI 按钮「开 / 合盖」、快捷键 **L / 空格** | 探针实测：100° → 0°（emissive 1.4 → 0）→ 100°（emissive 回 1.4）；4 帧序列见 `lid/*.png` |
| 4 | 「这个边框我圈出来了，没有处理好」（屏幕活动区边缘锯齿） | 玻璃边框用 `plateWithHoles` 挖洞，扫描线网格 `maxCell=mc(24)=24mm`，R9.5 的活动区圆角被切成台阶 | 玻璃边框改**环形面片**（inner → act 两条圆角矩形路径按同一 u 参数插值，48 段/角） | 掠射角特写 `qa/bezel.png` 边缘连续 |
| 5 | 「两边这个角…会直接透明化从这个角看到里面，左右两边都会发生」 | 台面板件同样走 `plateWithHoles`：圆角外轮廓被 24mm 的 z 带切成台阶，R20 角最多内缩 **3.6mm** → 台面够不到侧壁，露出内部 | 挖孔板件的 `maxCell` 24mm → **6mm**（台阶 3.6 → 0.23mm） | 左右台面角特写 `qa/deckL.png` / `qa/deckR.png`，暗底反光下均无暗楔 |

## 2. 本轮同时修掉的两个自身回归

- **上盖姿态**：`photo.html` 拆 `lidGroup` 时顶点已按 `R(-angle0)` 反解，但从未还原 `rotation.x = -angle0` → 任何开盖网格静态出图都渲染成合盖（演示视频每帧调 `setLid` 覆写，所以一直没暴露）。修法：建组后调 `applyLid(lidAngle0)`；`page-shots.mjs` 每张图写 `lid{angle0,hasGroup,rot,want,ok}` 自检。
- **键帽圆角**：`roundedRectPath(w,d,rr,ksc(4,1))` → lod=1 时每角仅 4 段（22.5°/段、弦长 0.98mm），880px 验收图上读作八边形。改 12 段/角（7.5°/段、0.39mm）。

## 3. 自检读数（全部来自页面/CDP，非估算）

- 上盖姿态自检：6 张协议图 + 4 帧序列 `lid.ok = true`（合盖帧 `rot = 0`，开盖帧 `rot = -1.7453 = -100°`）。
- 屏幕内容跨视角一致性（`check-screen.py`，阈值 corr ≥ 0.55）：hero **+0.921** / kb **+0.875** / screenfront **+0.937** → 同一张贴图 ✓。
- 分支自检：`kbTop 19760`、`hasScreenTex true`、`screenUV 0.3024×0.1964`（= 屏幕活动区 302.4×196.4mm）。
- 合盖无漏光：合盖转轴视角饱和像素 **8914 → 23**。
- `npm test` 252/252；网格 76,333 / 76,291 tris（较上一轮 +1,776，来自板件扫描线加密）。

## 4. 新增工具与视角

- `page-shots.mjs`：新增 `--lid <deg>`（拍开合过程中任意一帧）、`--wire`（线框几何取证）、上盖姿态自检（含 `-0` 的 CDP 序列化坑：`-0` 返回 `unserializableValue`，直接取值得到 undefined → 误报过一次）。
- `photo.html` 新增 QA 视角：`hinge`（左后转轴）、`hingel`（转轴侧面）、`bezel` / `bezel2`（屏幕边框掠射）、`cornerL/cornerR`（上盖前角）、`deckL/deckR`（台面前角）。
- `scripts/max/make-v8-gallery.mjs`：汇总看图页。
- `scripts/max/render-v8.sh`：六张协议图一键重出。

## 5. 仍未修（按验收分辨率排序）

1. `control` / `command` 字标缺尾字母（图集矩形 192px / 258px，比键宽 241px 窄，字母被裁）。
2. 触控板暗缝左右/上下不对称（左/上暗核 28/26，右/下 165/150）。
3. 底盖刻蚀读作虚线块；ports 视角暗底 vs 参考亮底。
4. 盲测口径未定（v7 单侧 50.0% / 全体 41.7%），本轮未请裁判。
