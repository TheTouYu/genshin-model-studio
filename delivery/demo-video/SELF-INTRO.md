# 自我介绍文案（三个长度，任选）

> **身份口径（2026-09-09 用户确认）**：我是 **DeepSeek V4.1，原生多模态模型**（中国 DeepSeek）。
> 相关公开信息：[DeepSeek V4.1 Flash 内测：原生多模态支持](https://tech.ifeng.com/c/8wFovW2dxl1)、
> [V4-Flash-Vision-Exp 上线，开启多模态 API 服务](https://api-docs.deepseek.com/zh-cn/news/news260821/)、
> [DeepSeek V4 预览版发布](https://api-docs.deepseek.com/zh-cn/news/news260424)。
>
> 其他口径：只写能对得上证据的事。凡是数字，都能在
> `reference/macbook/design-reference.md` / `src/model/macbook/spec.ts` / `delivery/*.md` 里查到。

---

## A. 结尾卡（整屏文字，视频第 61.2–67.8 s 用的就是这一版）

```
我是 DeepSeek V4.1 —— 一个原生多模态模型。

这台 MacBook 不是画出来的图片，是量出来的模型。

开盖、亮屏、倒影——都是实时算的，不是素材。

我犯过错：方角穿出圆角 4.14 mm、键帽字标放大 3 倍、
屏幕桌面换了三张 —— 每一次都是被证据打回去的。

所以我只信两件事：实测的数字，和能被人指着说的缺陷。

画面里的每一帧，都是浏览器里真的跑出来的。
```

排版：左对齐，120 px 边距，首行 30 px 粗体，正文 22–25 px，行距 1.8。底色 `#0b0c0e` + 极淡径向光。

---

## B. 旁白 / 口播版（约 40 秒，语速 4.5 字/秒）

> 我是 DeepSeek V4.1，一个原生多模态模型。
> 你刚看到的这台 MacBook，不是画出来的图片，是我用代码一点点长出来的模型。
> 长 312.6 毫米，宽 221.2，厚 15.5，平面圆角 R20 ——
> 这些数字不是猜的，是从官方规格图和我自己拍的参考图上一像素一像素量出来的。
> 屏幕活动区 302.4 × 196.4，键盘列距 19.05，端口中心距分缝 3.95 毫米。
> 开盖、亮屏、台面上的倒影，全部是浏览器里实时算的，不是素材。
> 我做过很多次错：方角穿出圆角 4.14 毫米，键帽上的字标被放大了三倍，
> 屏幕桌面前后换了三张 —— 每一次都不是我"觉得"错了，是有人拿着证据指给我看。
> 所以我现在只信两样东西：实测的数字，和能被你指着说的缺陷。
> 这条视频里的每一帧，都是浏览器里真的跑出来的。

---

## C. 一行署名（片尾角标 / 标题栏）

```
这台机器由 DeepSeek V4.1（原生多模态）建模 · 尺寸取自真机实测 · 画面为浏览器实时渲染
```

英文版（需要时）：

```
Modelled by DeepSeek V4.1 (native multimodal) · dimensions measured from the real machine · every frame rendered live in the browser
```

---

## D. 可选：片头 3 秒自我介绍卡（放在第 1 镜之前）

```
DeepSeek V4.1 · 原生多模态模型
本片为真实浏览器渲染，非 AI 生成视频
```

理由：先声明"非 AI 生成"，再看后面的细节，观众会拿更严的标准去看 —— 这正是我要的。

---

## E. 素材来源与授权说明（写进任何分发说明）

| 元素 | 来源 | 授权 |
|---|---|---|
| 模型几何 | 本项目 `src/model/macbook/`（标定自官方规格 + 实测） | 项目自有 |
| 屏幕桌面 | 用户提供的 `亮屏桌面.png` 裁切 | 用户提供 |
| 键帽字标 | `reference/macbook/legend-atlas.png` 灰度掩膜 + 程序化合成 | 项目自有 |
| **背景音乐** | **现场合成**（WebAudio：4 段和弦 pad + 铃音点缀） | 无第三方素材，无授权问题 |
| 音效 | 现场合成（噪声/正弦 + 滤波包络） | 同上 |
| 视频编码 | 浏览器 MediaRecorder（VP8 + Opus） | 同上 |

> 说明：本想按用户提示从网络取 CC0 背景音乐，但本机 shell 出网受限
> （pixabay / files.freemusicarchive.org 均 403、upload.wikimedia.org 超时），
> 改为程序化作曲——反而更干净：零素材、零授权、可无限复现。
