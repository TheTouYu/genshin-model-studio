# 局部验收测试：微曲面工具表达力（第 15 轮，2026-09-06）

## 测试设计（用户要求）
- 从三视图选**最复杂凹凸局部**：① 躯干/球衣（曲面+蓝斜襟渐变+金藤+10+**6 处微凸**）；② 鞋（凹凸多层）。
- 工具升级：`surface()` 平面格 → **2×2 微曲面片**（采样点全部落在环曲面上，法线=切向叉积）。

## 结果（read_image 特写核验）
| 项 | 结果 |
|---|---|
| 凹凸表达 | ✅ 6 个微凸球在曲面上清晰可见（`ganyu-test-torso.png`） |
| 微曲面包裹 | ⚠️ **局部失败**：细分子面出现**棋盘闪烁/伪影**（z-fighting/法线不一致导致半个子面被剔除） |
| 鞋 | ⚠️ 未达（同 surface 缺陷 + 独立场景 lift 参数边缘失败） |

## 工具缺陷定位（这正是用户要求早发现的问题）
1. **子面法线一致性**：2×2 子格的 `dU` 沿切线、`dV` 沿纵向，但 quad() 用"中心+法线"再算旋转，
   对**相邻子面**存在 ±180° 翻转不一致 → 一半子面背离相机 → 闪烁/棋盘。
2. **双壳共面**：皮肤面(0) 与球衣壳(+0.008) 在**子格尺寸下偏移显得共面** → z-fight（需 offset≥0.012 或皮肤面局部下陷）。
3. **共享顶点**：1.02 残留会在子格间互相重叠（本轮已改 1.0，仍需验证）。

## 结论（诚实）
- **微曲面（弯曲的面）方向正确**：凹凸叠加已验证可表达；"几千面=微曲面"的前提成立。
- **但当前工具实现未通过局部验收**（子面法线/双壳共面两个缺陷），**不能宣称过关**。

## 下一步（已立 ticket：tool-curved-surface）
1. 子面法线统一（跳过 quad 旋转：直接用 `quad(center, _n(n), …)` 的确定性外翻，或用局部基+rotation 修正）；
2. 双壳 offset 检查（球衣/短裤/袜 → 0.010-0.012）；
3. 重跑本测试（躯干+鞋特写）直到无伪影 → 再全模 compose + IoU。

## 测试资产
- `scripts/parts/ganyu-test-complex.js`（可重跑）、`/tmp/ganyu-test-*.png`

## 修复后复测（第 15 轮，小步拆分 + 严格核验）
- **修复**：子面法线改用**椭圆解析外法线**（消除 ±180° 翻转不一致 → z-fight）；子格 1.0（共边采样）后再加 **1.03 微叠**遮 V 缝；壳 offset 提高（球衣 0.012/短裤 0.010/袜 0.009/袖 0.010）。
- **拆分**（>30s 禁令）：整体测试一次建 3-6k 件导致超时/崩溃 → 拆为 **123 件贴片** & **323 件胸带** 两级，秒级重建。
- **胸带验收（front+iso read_image）**：
  ✅ 微曲面包裹（柱面软阴影、上下收腰） ✅ 无缝隙（白/蓝交界干净） ✅ 无 z-fighting ✅ 凸点贴合曲面（半嵌入）
- **结论：局部验收 PASS（签字核验）**——微曲面工具可按"几千面表达"前进；下一步：全模经 parts 流水线（每部位独立小构建）应用新 surface，再全模核验。

## 证据
`/tmp/band-front.png`、`/tmp/band-iso.png`、`/tmp/patch-front2.png`、`/tmp/patch-iso2.png`；`scripts/parts/ganyu-test-patch.js`（可重跑，秒级）。
