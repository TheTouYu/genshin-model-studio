# 甘雨三视图还原 — 完整问题诊断（API/工具层 + 局部细节层）

> 依据：用户提供的【材质与纹样局部拆解设定图】（蓝布/白布/黑紧身/金边/红绳结/宝石/角分节）+
> 【三视图全身体检】。本轮**只诊断，不改代码**。

---

## 第一部分：我自己画的局部（胸-腰承托面）——逐项对照拆解图的失败点

| # | 失败点 | 拆解图标准 | 当前实现 | 根治（工具/方法） |
|---|---|---|---|---|
| D1 | **曲面是"拼板"不是"布料"** | 布面连续、光泽渐变 | 每子格=平面+单色，硬面片带、边界阶梯 | 需要**平滑着色**：单元越小+颜色按位置连续插值（lerpColor）→ 视觉连续；或升级渲染：顶点法线的软表面原语 |
| D2 | **蓝布渐变是硬色块** | 白→浅蓝→深蓝**连续渐变**，带光泽 | C1..C4 四档硬边界 | 工具级 `lerpColor(a,b,t)` + 每格插值；光泽用**高光条**（浅色细片贴布面） |
| D3 | **金藤不像纹样** | 金线**枝蔓**：缠绕、分叉、粗细变化 | 单链正弦 rod | 工具级 `vine(path)`：分叉链+粗细渐变+节点小球 |
| D4 | **金边不是"金属雕刻"** | 领口/下摆**包边金线**+雕线 | 简单金色细杆弧 | 工具级 `edgeTrim(ringPath, width)`：沿边界自动铺金边+**内雕线**（双线） |
| D5 | **红绳结是两根棍** | **绳结结体**（环状）+ 红流苏多条 + 金扣 | 2 根红杆 + 小球 | 工具级 `tassel()`：结环 + 3-4 股流苏 + 金扣 + 宝石座 |
| D6 | **宝石是圆球** | **刻面宝石**（八面体/菱形）+ 透明的多面反光白点 | sphere 圆球 | 工具级 `gem()`（2-3 片 tri 组成八面体 + 高光点） |
| D7 | **角无分节** | 角表面**分节圆环纹理**（每节弧度+明暗） | 三段色带 | 工具级 `hornSegment(path, n)`：沿角路径做 n 节截面环（明暗交替+微凸） |
| D8 | **10 是平面贴片** | 数字**金属浮雕**+金边 | 平面 disc+rod | 数字用 rod 线框 + 金边描点 + 高光 | 
| D9 | 纹样位置错误 | 徽章/鸢尾/流苏按原图**精确位置+贴合曲面** | 手写 z 硬贴（有的浮空/倾斜） | 工具级 `decal(obj, anchor)`：按曲面**法线/切向**自动定位贴合 |
| D10 | 网格色尚未表达"黑紧身/皮革/金属"的**质感差异** | 皮面无反光 / 金属高反光 / 麻布哑光 | 一律平色 | 工具级 `material('satin'|'leather'|'metallic'|'jade')`：输出**基色+高光+暗部**多片预设 |

## 第二部分：API/工具层通用问题（这是关键——先改方法，再画什么都快）

### T1. 缺"沿路径放样"原语（最致命）
现状：`surface()` 只支持**竖向回转环**。但角（沿弯月路径的变截面体）、鞋（沿脚长截面）、袖口/飘带/绳结都不是回转体；
所以这些部件只能"手拼平面/杆"→ 形状永远歪、慢、不可复刻。
**通用方案**：新增 `loft(path, sections, colorFn)` —— 路径(曲线控制点) + 截面(椭圆/圆/圆角矩形，大小随路径变化) → 自动出弯曲变截面网格（微曲面子片版）。
   - 用途链：角(弯月路径+圆截面+分节)、鞋(脚长路径+圆形截面,脚趾/跟放大)、袖(臂路径)、流苏(绳路径)、马尾(单根发丝路径)——**一次实现全组件复用**。

### T2. 缺"曲面贴花/曲面定位"系统
任何纹样（鸢尾/金藤/10/GANYU/徽章）都必须**贴在曲面上**（法线+切向定位），现在全是手算 z/rotation。
**通用方案**：`decal(colorFn|items, {u,v})` 把贴花以 **UV 参数**贴到 surface 网格上（自动求该点法线/切向、自动微抬 0.0005 防 z-fight），
并提供 `decalCheck()` 自动校验"贴花平均脱离距离 ≤ 0.002"——把"肉眼找浮空"变成断言。

### T3. 缺"材质/颜色插值"层
`lerpColor(c1,c2,t)` + `materialPreset(kind)`（哑光布/缎面/皮革/金属/宝石/红绳）+ **每单元自动混色** → 渐变、光泽、质感差异全部**声明式**，不再手挑色。

### T4. 公共库缺失（10 份重复代码）
surface/offsetRings/ribbon/quad/_n 等 helper **在 10 个 part 文件里各复制一份**：
改一个 surface 升级要动 10 处（这轮已实地踩过：改 10 文件、易漏、回归难）。
**通用方案**：`scripts/parts/lib/ganyu-lib.js`（或 `window.GYU_LIB` 注入）——所有原语集中一份；
parts 只写"画什么"，库负责"怎么画"。配套 `make-part(partName)` 模板快速搭新部位。

### T5. 增量构建/性能（>30s 禁令的工程化）
每个 part 全量 regenerate（3-6k 件 1-2 分钟）→ 违反"小步快跑"。
**通用方案**：
   - part 级 **build cache**（仅变化 part 重建，未变化复用 parts/<id>.json 已有能力，已在 parts-tool 实现雏形）；
   - **dirtyIds 增量**：draw-model 支持只重算变化 strokes（前端 diff）；无变化部位 items 直接缓存复用；
   - 每步操作 ≤30s：默认"单部位构建 + 特写"模式，全模 compose 是显式动作。

### T6. 渲染/核验套件不标准
- **页面稳定性 bug**（刚发生）：preview-demo 与主页 route 混乱导致 `window.gms undefined`——init 需**显式路由校验 + gms 就绪等待 + 自动重载 fallback**；
- 缺少**自动正交特写矩阵**（front/side/top/3-4 × 部位）+ 与拆解图**并排对比图**自动生成（现在手工 setTarget/截图）。
**通用方案**：`render-suite(part)` 一个命令输出该部位五特写 + 与参考 detail 并排 PNG；`verify-suite` 断言 缝隙≤1px/无浮空/渐变连续等。

### T7. 设计方法本身（"怎么复刻一个部件"的通用步骤）
把"画甘雨"变成可复刻工序：
1. 参考拆解图 → **部件拆解单**（几何体+材质+纹样+位置）；
2. 选原语：回转面→surface；弯曲变截面→loft；纹样→decal；饰件→gem/tassel/edgeTrim/vine；
3. 分层：轮廓层（base）→ 材质层（lerpColor/高光）→ 贴花层 → 饰件层；
4. `render-suite(part)` 特写核验 → 不达标回原语层（改原语，不是改坐标）；
5. PASS 后 parts-tool 归档。

---

## 结论
- **画得不好不是手笨，是"方法-原语-工具"未成形**：D1-D10 全部可归为 T1-T6 六类原语缺失（loft/decal/材质/库/增量/核验套件）。
- **正确次序**：先实现 T1-T4 四个原语 + T7 工序（公共库一次到位）→ 用新原语**重新画胸-腰局部**（这次按拆解图逐项：渐变布、金编蔓、金边雕、绳结宝石、贴花贴合）→ render-suite 严格核验 → 再上全模。
- 本轮不改任何一行部位代码；等你批准该"工具设计蓝图"后开始实现（每个原语单独 ticket + 单独核验）。
